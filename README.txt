1) ÷ffnen sie eine Commandozeile
2) Pr¸fen sie mit 'node --version' und 'npm --version' ob NodeJS insatlliert ist.
	Wenn nicht muss NodeJs f¸r ihr Betriebssystem Installiert werden (https://nodejs.org/en/download/)
	f¸hren sie im Terminal 'npm install' aus
3) navigieren sie im Terminal in den Ordner des Repositories (RSS-FEED-MTR) 
4) starten sie 'npm install node-modules' und danach 'npm install -g @angular/cli' 
um die nˆtigen files und angular-strukturen zu laden

5)ˆffnen sie ein zweites Terminal und navigieren sie in den Ordner des Repositories/server (RSS-FEED-MTR)
6) f¸hren sie 'npm install node-modules' aus und npm install ibm-watson'
7) starten sie in diesem Terminal mit 'node server.ts' den Webserver auf localhost:5200
-> Der Server l‰uft! Hurra!! Wechseln sie wieder in das erste Terminal
8) starten sie mit 'ng s' dias Client-Seitige Frontend auf localhost:4200
-> Das Frontend l‰uft. Viel Spaﬂ bei der benutzung

Im Ordner Doku liegt der Erfahrungsbericht, das Flussdiagramm und die Screenshots
